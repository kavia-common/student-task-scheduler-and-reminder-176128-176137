# Streamlit Task Scheduler & Reminder App - Environment Variables

# Directory where the SQLite database will be stored
DATA_DIR=./data

# Enable or disable reminder notifications (true/false)
NOTIFICATIONS_ENABLED=true

# Background scheduler scan interval in seconds
SCHEDULER_INTERVAL_SECONDS=60
